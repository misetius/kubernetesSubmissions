apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
  namespace: project
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: standard-rwo